import React from "react";

const GetInTouchCard = ({ image, title, desc, subTitle }) => {
  return (
    <div className="w-[345px] h-[345px] shadow-sm rounded-full">hello</div>
  );
};

export default GetInTouchCard;
